services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./content:/app/content
      - ./templates:/app/templates
      - ./static:/app/static
    environment:
      - GIN_MODE=release
